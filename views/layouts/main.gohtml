{{define "main"}}
    {{template "header"}}
    <div class="container-fluid">
        {{if .Alert}}
            {{template "alert" .Alert}}
        {{end}}
        {{template "welcome"}}
        {{template "yield1"}}
    </div>
    {{template "footer"}}
{{end}}