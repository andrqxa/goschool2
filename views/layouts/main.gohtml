{{define "main"}}
    {{template "header"}}
    <div class="container-fluid">
        {{if .Alert}}
            {{template "alert" .Alert}}
        {{end}}
        {{template "welcome"}}
        {{template "newMember"}}
        {{template "allUsers"}}
    </div>
    {{template "footer"}}
{{end}}